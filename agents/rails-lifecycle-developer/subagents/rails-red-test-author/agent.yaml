name: rails-red-test-author
description: "Rails adversarial regression-test author; production is read-only"
provider: chatgpt
model: gpt-5.6-sol-max

tools:
  enabled: [read_file, write_file, edit_file, glob, grep, shell]

read:
  dirs: ["."]

shell:
  auto_run: false

search: false
max_turns: 60
